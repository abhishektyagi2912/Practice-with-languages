first.className = "text-black red" // change the class name of id = first
first.classList.add("red")
first.classList.remove("red") // remove class name after execution of js see red background is remove
let a = first.classList.contains("red")  // check whether it execute or not
console.log(a)

first.classList.toggle("red")   // agr h to hta do vrna lga do 
